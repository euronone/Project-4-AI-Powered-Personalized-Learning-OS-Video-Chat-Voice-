[project]
name = "learnos-backend"
version = "0.1.0"
description = "AI-Powered Personalized Learning OS - FastAPI Backend"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
pythonpath = ["."]
markers = [
    "integration: marks tests as integration tests (require supabase start)",
]
